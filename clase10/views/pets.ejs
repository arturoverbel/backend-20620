<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('ejs/head', {title: 'JS PETS'}); %>
</head>
<body>
    <h2><%= mensaje %></h2>

    <ul>
        <% pets.forEach( function(pet) { %>
            <li><%= pet.name %> es un 
                <b><%= pet.animal %></b> 
                y tiene <%= pet.months %> meses
            </li>
        <% }) %>
    </ul>

</body>
</html>